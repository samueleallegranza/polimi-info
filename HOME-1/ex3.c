#include <stdio.h>
#define DIM 50

int main(void) {

    char str[DIM+1];
    char comp[(DIM*2)+1];
    int i, molt;

    scanf("%s", str);

    molt = 0;
    for(molt=0 ,i=0; i<DIM; i++)
        

    return 0;
}